import { DetectAllFacesImgDirective } from './detect-all-faces.directive';

describe('DetectAllFacesDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectAllFacesImgDirective();
    expect(directive).toBeTruthy();
  });
});
